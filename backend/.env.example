# StonePieHome Backend Configuration

# CORS allowed origins (comma-separated)
# Default: http://spark.local:8020,http://localhost:8020
CORS_ORIGINS=http://spark.local:8020,http://localhost:8020

# Add production origins as needed:
# CORS_ORIGINS=http://spark.local:8020,http://localhost:8020,https://yourdomain.com

# Enable actual system actions (restart/shutdown)
# Set to "true" to enable actual restart/shutdown commands
# Default: false (UI-only mode)
ENABLE_SYSTEM_ACTIONS=false
